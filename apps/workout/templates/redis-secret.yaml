# redis-secret must be pre-created manually before ArgoCD sync:
#
#   kubectl create secret generic redis-secret \
#     --from-literal=password='<strong-password>' \
#     -n workout
#
# This file is a placeholder comment; the actual Secret is not managed by ArgoCD
# to keep credentials out of git.
