zitadel:
  zitadel:
    masterkeySecretName: zitadel-masterkey

    # secretConfig is rendered into a K8s Secret and mounted in BOTH the init job and
    # main deployment — the only way to get passwords into the pre-install hook.
    secretConfig:
      Database:
        Postgres:
          User:
            Password: "y25r7rJ0akKbWRjeFNVw7xpge45uOpVE"
          Admin:
            Password: "Mj7QBasQ361RawDMjUbKfC1hoiyZfi2JwsmZo8NT44cAJPIarZMglXLSMgKJ8jPH"

    configmapConfig:
      ExternalDomain: zitadel.barrows.helegoeie.dev
      ExternalSecure: true
      ExternalPort: 443
      TLS:
        Enabled: false
      Database:
        Postgres:
          Host: workout-pg-rw.workout.svc.cluster.local
          Port: 5432
          Database: zitadel
          MaxOpenConns: 20
          MaxConnLifetime: 30m
          MaxConnIdleTime: 5m
          User:
            Username: zitadel
            SSL:
              Mode: disable
          Admin:
            Username: postgres
            SSL:
              Mode: disable
