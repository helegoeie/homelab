apiVersion: v1
kind: Service
metadata:
  name: {{ include "log01.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "log01.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "log01.selectorLabels" . | nindent 4 }}
