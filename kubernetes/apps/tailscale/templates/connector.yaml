apiVersion: tailscale.com/v1alpha1
kind: Connector
metadata:
  name: barrows-subnet-router
  namespace: tailscale
spec:
  hostname: barrows-router
  tags:
    - tag:k8s
  subnetRouter:
    advertiseRoutes:
      - "192.168.1.0/24"
