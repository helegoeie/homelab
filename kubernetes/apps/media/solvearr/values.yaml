app-template:
  controllers:
    main:
      containers:
        main:
          image:
            repository: ghcr.io/nabil-ak/solvearr
            tag: latest
          env:
            TZ: Europe/Amsterdam
  service:
    main:
      controller: main
      ports:
        http:
          port: 8191
